# Core
flask
werkzeug
requests
certifi
lxml

# PDF + DOCX generation (pro/quote_sheet exporter). Pure Python, no
# native deps — safe to bundle inside the .app via the existing
# site-packages tree.
reportlab>=4.0
python-docx>=1.1

# AI providers
# - Ollama is local; no package needed (install separately: https://ollama.com)
# - BYO API key support: official SDKs for Anthropic and OpenAI
anthropic>=0.42.0
openai>=1.60.0

# API keys live in the macOS Keychain (ai_providers/config.py routes through this)
keyring>=25,<26

# Security pins (SEC-06): transitive deps that pip-audit flagged with CVEs.
# Pinned to the patched releases (not >=) so the bundled site-packages is
# reproducible and ships clean. Re-audit on each dependency bump.
idna==3.15          # CVE-2026-45409
urllib3==2.7.0      # PYSEC-2026-141, PYSEC-2026-142
setuptools==78.1.1  # CVE-2024-6345 (RCE), PYSEC-2025-49, PYSEC-2022-43012
