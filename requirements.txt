# Core
flask
werkzeug
requests
certifi

# Transcription (Parakeet MLX — fastest on Apple Silicon)
parakeet-mlx

# Fallback transcription
openai-whisper

# AI Chat & Analysis (uses Ollama locally — no package needed)
# Install Ollama separately: https://ollama.com
